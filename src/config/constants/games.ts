import type { GamesIdMap } from "./types";

// The map of all the avaiable games and their IDs for generating a short referral url/sharelink
export const gamesIdMap: GamesIdMap = {
  1: { id: 1, path: "/defi-games/lamborghini-driver/" },
};
